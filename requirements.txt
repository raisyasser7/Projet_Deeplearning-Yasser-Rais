# Projet de fin de module - Deep Learning (EMSI 2025-2026)
# Environnement testé avec Python 3.10 - 3.13
#
# Installation :
#   python -m pip install -r requirements.txt
#
# Pour le support GPU (CUDA), installer torch depuis https://pytorch.org
# en suivant la commande adaptée à votre version de CUDA.

torch>=2.0
torchvision>=0.15
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12

# Téléchargement automatique des jeux de données Kaggle depuis les notebooks
kagglehub>=0.3

# Exécution des notebooks
jupyter>=1.0
ipykernel>=6.0
nbformat>=5.9
